language: python
python:
  - "3.5"
# command to install dependencies
# install:
#   - pip install -r requirements.txt
# command to run tests
script:
  - py.test # or pytest for Python versions 3.6 and up
